framework:
    messenger:
        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            # failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'

        routing:
            #todo: http-sessions doesn't work under cli. Install redis and use redis session to handle messages with async transport
            'App\Queue\Message\AsyncMessageInterface': async
            #'App\Queue\Message\ProcessUploadedFileMessage': sync